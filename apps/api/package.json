{
  "name": "@deploylite/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "db:verify:integration": "DEPLOYLITE_API_POSTGRES_INTEGRATION=1 vitest run --config vitest.config.ts src/postgres.integration.test.ts"
  },
  "dependencies": {
    "@deploylite/config": "workspace:*",
    "@deploylite/contracts": "workspace:*",
    "@deploylite/db": "workspace:*",
    "@deploylite/domain": "workspace:*",
    "fastify": "^5.2.1"
  }
}
